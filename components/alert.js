export const Alert = () => (
  <div className="bg-danger text-white text-center py-2 mb-4">
    {/* Sorry, these are the wrong credentials. Try again. */}
    The data is entered incorrectly.
  </div>
);
